# ✅ Простой бэкенд создан и готов к использованию!

## 🚀 Что создано:

### 1. ✅ FastAPI Backend (`backend/` папка)
- **main.py** - API сервер с эндпоинтами
- **requirements.txt** - Python зависимости
- **start.sh** - скрипт для быстрого запуска
- **README.md** - документация

### 2. ✅ API Endpoints:
- ✅ `POST /api/auth/login` - Вход
- ✅ `POST /api/auth/register` - Регистрация
- ✅ `GET /api/users/me` - Получить пользователя
- ✅ `GET /api/courses` - Получить курсы
- ✅ `GET /api/gap-reports/{user_id}` - Gap report
- ✅ `POST /api/roadmaps/generate` - Roadmap

### 3. ✅ iOS приложение обновлено:
- ✅ `baseURL` обновлен в `Constants.swift` → `http://localhost:8000`

## 📝 Как запустить бэкенд:

### Вариант 1: Автоматический (рекомендуется)
```bash
cd backend
./start.sh
```

### Вариант 2: Вручную
```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python main.py
```

Сервер запустится на: **http://localhost:8000**

## 🔧 Настройка iOS приложения:

### Для симулятора (уже настроено):
- `baseURL = "http://localhost:8000"` ✅

### Для реального устройства:
1. Узнайте IP адрес вашего Mac:
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```
   Или в System Preferences → Network

2. Обновите `Constants.swift`:
   ```swift
   static let baseURL = "http://192.168.x.x:8000"  // Замените x.x на ваш IP
   ```

3. Включите реальный API в `APIService.swift`:
   ```swift
   init(..., useMockData: Bool = false) {  // Изменить на false
   ```

## 📚 Документация API:

После запуска сервера:
- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc

## ✅ Готово!

**Бэкенд готов к использованию!**

**Запустите сервер и протестируйте iOS приложение!** 🎉
