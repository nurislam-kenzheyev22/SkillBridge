# ✅ Решение проблемы с Python!

## 🐍 Проблема:
Python 3.14 несовместим с pydantic-core

## ✅ Решения:

### Вариант 1: Упрощенная версия (БЕЗ pydantic) - РЕКОМЕНДУЕТСЯ

```bash
cd backend
./start_simple.sh
```

Использует `main_simple.py` и `requirements_simple.txt` (без pydantic)

### Вариант 2: Python 3.11/3.12

```bash
# Установить Python 3.11
brew install python@3.11

# Запустить
cd backend
rm -rf venv
python3.11 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
python main.py
```

### Вариант 3: Использовать системный Python 3.11/3.12

```bash
cd backend
rm -rf venv
python3.11 -m venv venv  # или python3.12
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
python main.py
```

## 🚀 Быстрый старт (упрощенная версия):

```bash
cd backend
./start_simple.sh
```

**Это самый простой способ!** ✅

Сервер: http://localhost:8000
Документация: http://localhost:8000/docs
