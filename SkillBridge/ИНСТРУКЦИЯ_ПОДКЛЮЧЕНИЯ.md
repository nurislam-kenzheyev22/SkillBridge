# 📱 Инструкция по подключению iOS к реальному API

## 🚀 Шаг 1: Запустить бэкенд

```bash
cd backend
./start_with_db.sh
```

Сервер запустится на: **http://localhost:8000**

Проверьте в браузере:
- http://localhost:8000/docs (Swagger UI)

## 📱 Шаг 2: Обновить iOS приложение

### Вариант 1: Изменить useMockData на false

В файле `APIService.swift` найдите:
```swift
init(networkService: NetworkServiceProtocol = NetworkService.shared, useMockData: Bool = true) {
```

Измените на:
```swift
init(networkService: NetworkServiceProtocol = NetworkService.shared, useMockData: Bool = false) {
```

### Вариант 2: Создать отдельный APIService для реального API

Можно создать отдельный экземпляр:
```swift
let realAPIService = APIService(useMockData: false)
```

## 🔧 Шаг 3: Проверить baseURL

В `Constants.swift` должно быть:
```swift
static let baseURL = "http://localhost:8000"
```

## ✅ Шаг 4: Протестировать

1. Запустите iOS приложение
2. Проверьте, что данные приходят с сервера
3. Проверьте в Swagger UI, что запросы приходят

## 🐛 Если не работает:

### Проблема: "Cannot connect to server"
- Убедитесь, что бэкенд запущен
- Проверьте, что порт 8000 свободен

### Проблема: "CORS error"
- CORS уже настроен в бэкенде
- Если проблема остается, проверьте настройки

### Проблема: "Data format mismatch"
- Проверьте, что модели соответствуют API ответам
- Проверьте в Swagger UI формат данных

## 📊 Проверка работы:

1. Откройте http://localhost:8000/docs
2. Попробуйте эндпоинты вручную
3. Проверьте базу данных: `sqlite3 backend/skillbridge.db`

## ✅ Готово!

После этих шагов iOS приложение будет работать с реальным API и базой данных!
