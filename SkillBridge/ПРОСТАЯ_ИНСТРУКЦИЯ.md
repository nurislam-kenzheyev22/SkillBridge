# 📱 Простая инструкция: Что делать дальше

## ✅ Хорошая новость:

**APIDecoders.swift НЕ НУЖЕН!** 

Стандартный JSONDecoder в NetworkService уже умеет работать с API.

## 🚀 Что нужно сделать:

### Шаг 1: Просто добавьте файл в Xcode (если еще не добавили)

1. Откройте Xcode
2. В левой панели найдите папку `Core/Network`
3. Правой кнопкой на `Core/Network` → "Add Files to SkillBridge..."
4. Найдите файл `APIDecoders.swift` (если он есть)
5. **НО!** Если файла нет - это нормально! Он не нужен!

### Шаг 2: Проверьте, что useMockData = false

В файле `APIService.swift` строка 35 должна быть:
```swift
init(..., useMockData: Bool = false) {
```

### Шаг 3: Запустите приложение

1. Убедитесь, что бэкенд запущен: `cd backend && ./start_with_db.sh`
2. Запустите iOS приложение в Xcode
3. Все должно работать!

## ✅ Готово!

**Если APIDecoders.swift вызывает проблемы - просто удалите его!**
Стандартный декодер справится сам.
