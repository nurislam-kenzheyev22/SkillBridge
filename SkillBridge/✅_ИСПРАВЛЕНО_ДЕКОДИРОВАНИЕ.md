# ✅ Исправлено декодирование Course

## 🔧 Проблема:

API возвращает:
- `id` как строку (UUID в виде строки)
- `skills` как массив строк `["Swift", "SwiftUI"]`, а не UUID

А модель Course ожидала:
- `id` как UUID
- `skills` как `[UUID]`

## ✅ Решение:

Добавлен кастомный `init(from decoder:)` в `Course.swift`, который:
1. Преобразует строковый UUID в UUID
2. Преобразует массив строк skills в массив UUID (генерирует новые UUID)

## 🚀 Что делать:

1. **Перезапустите приложение в Xcode**
   - ⏹️ Stop
   - ▶️ Run

2. **Откройте Course Catalog**
   - Курсы должны загрузиться!

## ✅ Готово!

Теперь декодирование должно работать!
