# 🎯 Что делать - пошаговая инструкция

## ✅ Шаг 1: Проверить бэкенд

Откройте терминал и выполните:
```bash
cd ~/SkillBridge/backend
./start_with_db.sh
```

Должно появиться:
```
✅ Starting server on http://localhost:8000
```

## ✅ Шаг 2: Проверить в Xcode

1. Откройте проект `SkillBridge.xcodeproj` в Xcode
2. Найдите файл `APIService.swift` (в папке `Core/Network`)
3. Откройте его
4. Найдите строку 35 (должно быть примерно так):
   ```swift
   init(networkService: NetworkServiceProtocol = NetworkService.shared, useMockData: Bool = false) {
   ```
5. Если там `true` - измените на `false`

## ✅ Шаг 3: Запустить приложение

1. В Xcode нажмите ▶️ (Run)
2. Выберите симулятор (например, iPhone 15)
3. Дождитесь запуска
4. Откройте Course Catalog
5. Курсы должны загрузиться из базы данных!

## ❌ Если APIDecoders.swift мешает:

1. Найдите файл `APIDecoders.swift` в Xcode
2. Выделите его
3. Нажмите Delete
4. Выберите "Move to Trash"
5. Он не нужен - стандартный декодер работает сам!

## ✅ Готово!

Все должно работать без APIDecoders.swift!
