# ✅ Простой бэкенд создан!

## 🚀 Что создано:

### 1. ✅ FastAPI Backend
- **Расположение:** `backend/` папка
- **Файлы:**
  - `main.py` - основной API сервер
  - `requirements.txt` - зависимости Python
  - `start.sh` - скрипт для запуска
  - `README.md` - документация

### 2. ✅ API Endpoints:
- `POST /api/auth/login` - Вход
- `POST /api/auth/register` - Регистрация
- `GET /api/users/me` - Получить пользователя
- `GET /api/courses` - Получить курсы
- `GET /api/gap-reports/{user_id}` - Gap report
- `POST /api/roadmaps/generate` - Roadmap

### 3. ✅ Особенности:
- CORS настроен для iOS приложения
- In-memory хранилище (простое)
- Swagger документация автоматически
- Готов к расширению

## 📝 Как запустить:

```bash
cd backend
./start.sh
```

Или вручную:
```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python main.py
```

## 🔧 Для iOS приложения:

Нужно обновить `baseURL` в `Constants.swift`:
- Для симулятора: `http://localhost:8000`
- Для реального устройства: `http://192.168.x.x:8000` (IP вашего компьютера)

И изменить `useMockData = false` в `APIService.swift`

## ✅ Готово!

**Простой бэкенд готов к использованию!** 🎉
