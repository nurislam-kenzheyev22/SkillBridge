# ✅ Решение проблемы с APIDecoders.swift

## 🔧 Что изменено:

### Проблема:
Переопределение `init(from decoder:)` для `Codable` структур вызывало конфликт.

### Решение:
Вместо переопределения `init(from decoder:)`, созданы helper функции:
- `Roadmap.fromAPIResponse(_:)`
- `RoadmapStep.fromAPIResponse(_:)`
- `GapReport.fromAPIResponse(_:)`
- `SkillGap.fromAPIResponse(_:)`
- `Course.fromAPIResponse(_:)`

## 📱 Как использовать:

### Вариант 1: Добавить файл в Xcode
1. Откройте Xcode
2. Правой кнопкой на `Core/Network`
3. "Add Files to SkillBridge..."
4. Выберите `APIDecoders.swift`
5. Нажмите "Add"

### Вариант 2: Файл уже добавлен
Если файл уже виден в Xcode, но не компилируется:
1. Выберите файл в навигаторе
2. Проверьте Target Membership (правая панель)
3. Убедитесь, что "SkillBridge" отмечен

## ✅ Готово!

Файл теперь должен компилироваться без ошибок!
