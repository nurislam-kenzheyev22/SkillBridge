# SkillBridge

Интеллектуальная система для сопоставления университетских учебных программ с требованиями рынка труда и создания образовательных дорожных карт.

## 📱 О проекте

SkillBridge - это iOS приложение, которое помогает студентам:
- Анализировать пробелы в навыках между учебной программой и требованиями рынка труда
- Создавать персонализированные образовательные дорожные карты
- Находить подходящие курсы для развития навыков
- Отслеживать прогресс обучения

## 🏗️ Архитектура

### Frontend (iOS)
- **Язык:** Swift
- **Framework:** SwiftUI
- **Архитектура:** MVVM (Model-View-ViewModel)
- **Минимальная версия iOS:** iOS 17.0+

### Backend (API)
- **Язык:** Python
- **Framework:** FastAPI
- **База данных:** SQLite
- **Порт:** 8000

## 📁 Структура проекта

```
SkillBridge/
├── SkillBridge/                    # iOS приложение
│   ├── Core/                       # Основные компоненты
│   │   ├── Models/                 # Модели данных
│   │   ├── Network/                # Сетевой слой
│   │   ├── Storage/                # Хранение данных
│   │   ├── UI/                     # UI компоненты
│   │   ├── Utils/                  # Утилиты
│   │   └── Error/                  # Обработка ошибок
│   ├── Features/                   # Функциональные модули
│   │   ├── Welcome/               # Экран приветствия
│   │   ├── Onboarding/            # Онбординг
│   │   ├── Dashboard/              # Главный экран
│   │   ├── GapReport/             # Отчет о пробелах
│   │   ├── Roadmap/               # Дорожная карта
│   │   ├── CourseCatalog/         # Каталог курсов
│   │   ├── CourseDetail/          # Детали курса
│   │   ├── Profile/               # Профиль пользователя
│   │   └── Settings/              # Настройки
│   └── SkillBridgeApp.swift       # Точка входа
├── backend/                        # Backend сервер
│   ├── main_with_db.py            # FastAPI приложение с БД
│   ├── database.py                 # Модуль работы с БД
│   ├── requirements_simple.txt     # Зависимости Python
│   └── start_with_db.sh           # Скрипт запуска
└── README.md                       # Документация
```

## 🚀 Быстрый старт

### Требования

- **macOS** 13.0 или новее
- **Xcode** 15.0 или новее
- **Python** 3.11 или 3.12 (рекомендуется)
- **iOS Simulator** или физическое устройство

### Установка Backend

1. Перейдите в папку backend:
```bash
cd backend
```

2. Запустите скрипт установки:
```bash
./start_with_db.sh
```

Сервер запустится на: **http://127.0.0.1:8000**

**Документация API:** http://127.0.0.1:8000/docs

### Установка iOS приложения

1. Откройте проект в Xcode:
```bash
open SkillBridge.xcodeproj
```

2. Выберите симулятор (например, iPhone 15)

3. Нажмите ▶️ (Run) или ⌘+R

4. Приложение запустится и подключится к API

## 📊 База данных

### Структура БД

База данных SQLite создается автоматически при первом запуске бэкенда.

**Файл:** `backend/skillbridge.db`

### Таблицы

1. **users** - Пользователи
   - id, email, name, role, created_at

2. **courses** - Курсы
   - id, title, provider, description, duration_weeks, price, level, skills, url, rating

3. **gap_reports** - Отчеты о пробелах
   - id, user_id, readiness_score, skill_gaps, generated_at

4. **roadmaps** - Дорожные карты
   - id, user_id, title, status, estimated_total_hours, steps, created_at

### Просмотр данных

```bash
sqlite3 backend/skillbridge.db

# Просмотр таблиц
.tables

# Просмотр курсов
SELECT * FROM courses;

# Выход
.quit
```

## 🔌 API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход
- `POST /api/auth/register` - Регистрация

### Пользователи
- `GET /api/users/me` - Текущий пользователь

### Курсы
- `GET /api/courses` - Список всех курсов
- `POST /api/courses` - Создать курс

### Отчеты
- `GET /api/gap-reports/{user_id}` - Отчет о пробелах

### Дорожные карты
- `POST /api/roadmaps/generate` - Сгенерировать дорожную карту
- `PUT /api/roadmaps/{roadmap_id}/steps/{step_id}` - Обновить шаг

**Полная документация:** http://127.0.0.1:8000/docs

## 🎨 Дизайн

Приложение использует современный дизайн 2025:
- **Цветовая схема:** Purple Theme
- **Типографика:** SF Pro
- **Компоненты:** Переиспользуемые UI компоненты
- **Анимации:** Плавные переходы и прогресс-бары

## 🛠️ Разработка

### Архитектура iOS

- **MVVM** - Model-View-ViewModel
- **SOLID принципы** - Чистый код
- **Dependency Injection** - Через протоколы
- **Repository Pattern** - Для работы с данными

### Основные компоненты

- **NetworkService** - Сетевой слой
- **APIService** - API клиент
- **CacheManager** - Кэширование данных
- **KeychainManager** - Безопасное хранение токенов

### Модели данных

Все модели поддерживают кастомное декодирование из API:
- UUID из строк
- Enum из строк
- Даты из ISO8601 и других форматов

## 🐛 Решение проблем

### Проблема: "Cannot connect to server"

**Решение:**
1. Убедитесь, что бэкенд запущен: `cd backend && ./start_with_db.sh`
2. Проверьте URL в `Constants.swift`: должно быть `http://127.0.0.1:8000`
3. Для симулятора используйте `127.0.0.1`, а не `localhost`

### Проблема: "Decoding error"

**Решение:**
- Все модели имеют кастомные декодеры
- Проверьте формат данных в Swagger UI: http://127.0.0.1:8000/docs

### Проблема: Порт 8000 занят

**Решение:**
```bash
lsof -ti:8000 | xargs kill -9
```

## 📝 Лицензия

Этот проект создан для дипломной работы.

## 👥 Команда

- Kenzheyev Nurislam
- Yun Veronika
- Karashbekov Miras
- Ensezhar Arman

## 📚 Дополнительная документация

- [Инструкция по запуску бэкенда](backend/README.md)
- [Структура базы данных](backend/README_DB.md)

## ✅ Статус проекта

- ✅ iOS приложение готово
- ✅ Backend API готов
- ✅ База данных SQLite работает
- ✅ Интеграция frontend-backend работает
- ✅ Все ошибки исправлены

---

**Версия:** 1.0.0  
**Дата:** 2025
