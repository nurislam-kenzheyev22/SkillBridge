# ✅ Ошибка "Invalid redeclaration of 'CourseCard'" исправлена!

## 🎯 Проблема:
- `CourseCard` был объявлен дважды:
  1. В отдельном файле `CourseCard.swift`
  2. Внутри `CourseCatalogView.swift` (строки 349-411)

## ✅ Решение:
- Удален дубликат из `CourseCatalogView.swift`
- Оставлен только отдельный файл `CourseCard.swift` с NavigationLink

## ✅ Готово!

**Ошибка компиляции исправлена!**

**Теперь `CourseCard` объявлен только один раз в `CourseCard.swift`!** 🎉
