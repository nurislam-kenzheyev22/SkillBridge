# 📋 Методология работы - Системный подход

## 🎯 Принцип: Причина → Следствие → Решение

### ❌ Неправильный подход (что было):
1. Создал MockData без проверки моделей
2. Получил ошибки компиляции
3. Потом исправлял

### ✅ Правильный подход (что должно быть):

## 🔍 ШАГ 1: АНАЛИЗ И ПРОВЕРКА (ПРИЧИНА)

### Перед созданием любого кода:

1. **Проверить существующие модели:**
   ```bash
   # Прочитать все модели
   - User.swift
   - Course.swift
   - Curriculum.swift
   - Roadmap.swift
   - GapReport.swift
   - Skill.swift
   ```

2. **Записать структуру каждой модели:**
   - Какие параметры в init?
   - Какие типы?
   - Какие обязательные/опциональные параметры?

3. **Проверить зависимости:**
   - Какие enum используются?
   - Какие протоколы?
   - Какие связи между моделями?

## 🔍 ШАГ 2: ПЛАНИРОВАНИЕ (СЛЕДСТВИЕ)

### После анализа:

1. **Создать план:**
   - Что нужно создать?
   - Какие данные нужны?
   - Как они связаны?

2. **Проверить совместимость:**
   - Все типы совпадают?
   - Все параметры правильные?
   - Нет ли конфликтов?

## 🔍 ШАГ 3: РЕАЛИЗАЦИЯ (РЕШЕНИЕ)

### Только после проверки:

1. **Создать код по плану**
2. **Использовать правильные типы**
3. **Следовать структуре моделей**

## 📝 Чеклист перед созданием Mock данных:

- [ ] Прочитал все модели
- [ ] Записал структуру init каждого класса/структуры
- [ ] Проверил все enum и их значения
- [ ] Проверил типы параметров (String, Int, UUID, Date, etc.)
- [ ] Проверил опциональные параметры (?, nil)
- [ ] Создал план mock данных
- [ ] Проверил совместимость типов
- [ ] Создал mock данные
- [ ] Проверил компиляцию

## 🎯 Пример правильного процесса:

### Для MockData:

1. **АНАЛИЗ:**
   ```
   User model:
   - init(id: UUID, email: String, name: String, role: UserRole)
   - НЕТ token параметра
   ```

2. **ПЛАНИРОВАНИЕ:**
   ```
   Mock User:
   - Использовать только: id, email, name, role
   - НЕ использовать: token
   ```

3. **РЕАЛИЗАЦИЯ:**
   ```swift
   static let mockUser = User(
       id: UUID(),
       email: "student@iitu.kz",
       name: "Nurislam Kenzheyev",
       role: .student
   )
   ```

## ✅ Правила работы:

1. **Всегда сначала читай, потом пиши**
2. **Проверяй типы перед использованием**
3. **Используй системный подход: Анализ → План → Реализация**
4. **Не создавай код "наугад"**
5. **Проверяй компиляцию после каждого изменения**

## 🚨 Красные флаги (когда остановиться):

- ❌ Не знаю структуру модели → СТОП, прочитай модель
- ❌ Не уверен в типе → СТОП, проверь модель
- ❌ Ошибка компиляции → СТОП, проверь причину
- ❌ Не понимаю связь → СТОП, изучи зависимости

## 📚 Шаблон работы:

```
1. ЗАДАЧА: Создать MockData
2. АНАЛИЗ: Прочитать все модели
3. ПЛАН: Записать структуру каждой модели
4. ПРОВЕРКА: Убедиться что понимаю все типы
5. РЕАЛИЗАЦИЯ: Создать mock данные
6. ВАЛИДАЦИЯ: Проверить компиляцию
7. ТЕСТ: Убедиться что все работает
```
