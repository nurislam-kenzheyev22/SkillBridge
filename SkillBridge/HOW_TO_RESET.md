# 🔄 Как запустить приложение с нуля (без кэша)

## Способ 1: Через Settings в приложении (Рекомендуется)

### Шаги:
1. Запустите приложение
2. Завершите onboarding (или если уже завершен, перейдите в Settings)
3. Откройте **Settings** (последняя вкладка в Tab Bar)
4. Нажмите **"Reset Onboarding"**
5. Подтвердите сброс
6. **Перезапустите приложение** (⌘R в Xcode)

### Что делает:
- Очищает статус onboarding
- Очищает сохраненные университет и программу
- При следующем запуске покажется Welcome Screen

---

## Способ 2: Очистка UserDefaults через код

### В Xcode:
1. Откройте **Debug Console** (⌘⇧Y)
2. Введите команду:
```swift
UserDefaults.standard.removeObject(forKey: "hasCompletedOnboarding")
UserDefaults.standard.removeObject(forKey: "selectedUniversity")
UserDefaults.standard.removeObject(forKey: "selectedProgram")
UserDefaults.standard.synchronize()
```
3. Перезапустите приложение (⌘R)

---

## Способ 3: Очистка через Terminal

### Очистка UserDefaults симулятора:
```bash
# Найти bundle ID приложения
xcrun simctl get_app_container booted com.yourcompany.SkillBridge

# Очистить UserDefaults
xcrun simctl spawn booted defaults delete com.yourcompany.SkillBridge
```

### Или через Xcode:
1. **Product** → **Clean Build Folder** (⇧⌘K)
2. **Window** → **Devices and Simulators** (⇧⌘2)
3. Выберите симулятор
4. Нажмите **"Erase All Content and Settings"**

---

## Способ 4: Полная очистка (Сброс симулятора)

### Через Xcode:
1. **Window** → **Devices and Simulators** (⇧⌘2)
2. Выберите симулятор
3. Правой кнопкой → **"Erase All Content and Settings"**
4. Подтвердите

### Через Terminal:
```bash
# Список симуляторов
xcrun simctl list devices

# Сбросить конкретный симулятор
xcrun simctl erase "iPhone 16"

# Или сбросить все
xcrun simctl erase all
```

---

## Способ 5: Очистка кэша Xcode

### Очистка Derived Data:
```bash
# Удалить Derived Data
rm -rf ~/Library/Developer/Xcode/DerivedData

# Или через Xcode:
# Xcode → Preferences → Locations → Derived Data → стрелка вправо → удалить
```

### Очистка Module Cache:
```bash
rm -rf ~/Library/Developer/Xcode/iOS\ DeviceSupport
rm -rf ~/Library/Caches/com.apple.dt.Xcode
```

---

## Способ 6: Быстрый сброс (для разработки)

### Добавьте в код временную кнопку:

В `WelcomeView.swift` добавьте (для тестирования):
```swift
Button("Reset (Dev)") {
    UserDefaults.standard.removeObject(forKey: "hasCompletedOnboarding")
    UserDefaults.standard.synchronize()
    // Перезапустите приложение
}
```

---

## ✅ Рекомендуемый способ для разработки:

1. **Используйте Settings в приложении** (Способ 1)
2. Если нужно полная очистка → **Сброс симулятора** (Способ 4)

---

## 🔍 Проверка что сброс работает:

После сброса при запуске приложения вы должны увидеть:
- ✅ Welcome Screen (не Dashboard)
- ✅ Кнопки "Get Started" и "Sign up"
- ✅ Onboarding начинается с нуля

---

## 📝 Примечания:

- **UserDefaults** хранится в симуляторе/устройстве
- При удалении приложения все данные удаляются
- При сбросе симулятора все данные удаляются
- **Settings в приложении** - самый простой способ для тестирования
