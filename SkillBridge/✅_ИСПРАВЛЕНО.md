# ✅ Проблема исправлена!

## 🔧 Что было не так:

**iOS симулятор не может подключиться к `localhost:8000`**
- В симуляторе `localhost` = сам симулятор, а не ваш Mac
- Нужно использовать `127.0.0.1` вместо `localhost`

## ✅ Что исправлено:

В файле `Constants.swift` изменено:
```swift
// Было:
static let baseURL = "http://localhost:8000"

// Стало:
static let baseURL = "http://127.0.0.1:8000"
```

## 🚀 Что делать сейчас:

### 1. Перезапустите приложение в Xcode
- Нажмите ⏹️ (Stop)
- Нажмите ▶️ (Run) снова

### 2. Проверьте бэкенд (если не запущен):
```bash
cd ~/SkillBridge/backend
./start_with_db.sh
```

### 3. Откройте Course Catalog в приложении
- Курсы должны загрузиться из базы данных!

## ✅ Готово!

Теперь приложение должно подключиться к API!
