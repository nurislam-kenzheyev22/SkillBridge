# ✅ Исправлено декодирование GapReport

## 🔧 Проблема:

API возвращает `skillGaps` без поля `skillId`, а модель `SkillGap` требовала это поле.

## ✅ Решение:

1. **Добавлен кастомный декодер для `GapReport`:**
   - UUID из строки для `id` и `userId`
   - Обработка даты в формате "yyyy-MM-dd HH:mm:ss"
   - Декодирование массива `skillGaps`

2. **Добавлен кастомный декодер для `SkillGap`:**
   - UUID из строки для `id`
   - `skillId` опционален - если нет в API, генерируется новый UUID
   - Priority из строки

## 🚀 Что делать:

1. **В Xcode нажмите ⌘+B (Build)**
   - Ошибка декодирования должна исчезнуть!

2. **Запустите приложение ▶️**
   - Gap Report должен загрузиться!

## ✅ Готово!

Декодирование исправлено!
