# 🚀 Полная инструкция: База данных + API + iOS

## ✅ Что готово:

### 1. ✅ SQLite база данных
- **database.py** - модуль для работы с БД
- **Таблицы:** users, courses, gap_reports, roadmaps
- Автоматическая инициализация

### 2. ✅ API с базой данных
- **main_with_db.py** - API сервер
- Все данные сохраняются в БД
- Эндпоинты готовы

### 3. ✅ iOS декодеры
- **APIDecoders.swift** - преобразование API → iOS модели
- Обработка UUID, дат, enum

## 🚀 Шаг 1: Запустить бэкенд

```bash
cd backend
./start_with_db.sh
```

Проверьте:
- http://localhost:8000/docs (Swagger UI)
- http://localhost:8000/api/courses (должны быть курсы)

## 📱 Шаг 2: Подключить iOS

### Вариант A: Изменить useMockData

В `APIService.swift` строка 35:
```swift
init(..., useMockData: Bool = false) {  // Изменить true на false
```

### Вариант B: Создать отдельный экземпляр

В ViewModels:
```swift
let apiService = APIService(useMockData: false)
```

## 🔧 Шаг 3: Проверить baseURL

В `Constants.swift`:
```swift
static let baseURL = "http://localhost:8000"
```

## ✅ Шаг 4: Протестировать

1. Запустить iOS приложение
2. Проверить Dashboard - данные должны прийти с сервера
3. Проверить Courses - курсы из БД
4. Проверить в Swagger UI - запросы должны приходить

## 🗄️ Просмотр базы данных:

```bash
sqlite3 backend/skillbridge.db

# Просмотр таблиц
.tables

# Просмотр курсов
SELECT * FROM courses;

# Просмотр пользователей
SELECT * FROM users;
```

## 🐛 Решение проблем:

### Проблема: "Cannot connect"
- Проверьте, что бэкенд запущен
- Проверьте порт 8000

### Проблема: "Decoding error"
- Проверьте APIDecoders.swift добавлен в проект
- Проверьте формат данных в Swagger UI

### Проблема: "CORS error"
- CORS уже настроен
- Проверьте настройки

## ✅ Готово!

**База данных + API + iOS готовы к работе!** 🎉
