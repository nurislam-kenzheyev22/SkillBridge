# ✅ Самое простое решение

## 🎯 Что делать (3 простых шага):

### 1️⃣ Убедитесь, что бэкенд запущен

В терминале:
```bash
cd ~/SkillBridge/backend
./start_with_db.sh
```

Должно быть: `✅ Starting server on http://localhost:8000`

### 2️⃣ В Xcode - проверьте APIService.swift

1. Откройте файл `APIService.swift` (в папке `Core/Network`)
2. Найдите строку 35
3. Должно быть: `useMockData: Bool = false`
4. Если `true` - измените на `false`

### 3️⃣ Запустите приложение

1. В Xcode нажмите ▶️ (кнопка Run)
2. Выберите симулятор
3. Дождитесь запуска
4. Откройте Course Catalog
5. Курсы должны загрузиться!

## ❌ Про APIDecoders.swift:

**ЕГО НЕ НУЖНО ДОБАВЛЯТЬ!**

Если он мешает:
- Просто удалите его из Xcode (Delete → Move to Trash)
- Стандартный декодер работает сам

## ✅ Готово!

Все должно работать!
